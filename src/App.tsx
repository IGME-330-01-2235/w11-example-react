import { ToDoList } from './ToDoList';

function App() {
  return (
    <main>
      {/* It's nice to hide complexity inside a component. */}
      <ToDoList />
    </main>
  );
}

export default App;
